import 'package:flutter/material.dart';


class GetHome extends StatelessWidget {
  const GetHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Get-x Api'),
      ),

    );
  }
}
